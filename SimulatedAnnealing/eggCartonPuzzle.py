from simulatedAnnealing import SA

class Node:
    M, N, K, board = None, None, None, None
    def __init__(self,board):
        self.board = board
        M = len(board)
        N = len(board[0])
    def F(): #objectiveFunction():

    def Gn(): #generate neighbors

# M: rows; N: columns; K: (row, column or diagonal) no more than K eggs
def eggCarton(M, N, K): 
    carton = [[0]*M]*N

