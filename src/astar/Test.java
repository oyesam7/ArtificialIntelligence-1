package astar;

public class Test {
	public static void main(String[] args) {
		// ArrayList<ArrayList<Character>> matrix;
		SearchNode sn = new SearchNode("board-1-1");
		// matrix = sn.getMatrix();
		// for (int i = 0; i < matrix.size(); i++) {
		// for (int j = 0; j < matrix.get(0).size(); j++) {
		// System.out.print(matrix.get(i).get(j).toString() + " ");
		// }
		// System.out.println();
		// }
	}
}
